{
  "name": "@musicorum/coloris",
  "version": "1.0.0",
  "description": "an all-rounder back-end framework for musicorum services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "swc src -d dist --source-maps",
    "build:watch": "swc src -d dist --watch --source-maps",
    "build:types": "tsc",
    "prepublish": "npm run build && npm run build:types",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.36",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "redis": "^4.6.4"
  }
}
